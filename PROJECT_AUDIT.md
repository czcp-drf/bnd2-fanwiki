# 프로젝트 점검 — 2026-09-12

## 범위와 검증 결과

PROJECT.md와 공개 페이지, 지도, 클립 플레이어, 관리자 인증·저장 액션, 제보 처리, DB 스키마·마이그레이션, 테스트 설정을 검토했습니다. 실제 DB 변경·제보 제출·마이그레이션·배포는 수행하지 않았습니다. 모든 화면의 브라우저 상호작용이나 운영 DB 정책을 검증한 것은 아닙니다.

- `npm run build`: 성공 (TypeScript 포함).
- `npm run lint`: 실패 — 오류 6개, 경고 8개.
- `npm run test:reliability`: 10개 중 8개 성공, 2개 실패. 제보 테스트의 `next/headers` 모의 의존성이 누락되어 실행 단계에서 실패합니다.
- 별도 메모리 내 모의 실행: 제보의 File 입력 예외와 추가 필드의 길이 제한 우회를 확인했습니다. 실제 DB 호출 없음.

## 우선 수정할 문제

### 1. 퇴장한 멤버가 현재 조직 멤버에 계속 포함됨 — 높음

**수정 완료 (fix/project-audit, 1차)**: 상위 멤버 조회에 left_at을 추가하고 현재 활동/현재 비활동/이전 멤버로 분리했습니다. 헤더 인원 수는 현재 활동+비활동 합계로 집계해 조직 목록의 현재 소속 기준과 맞췄습니다. 이전 멤버는 별도 섹션과 퇴장 배지로 표시합니다. 퇴장·복귀·비활동·누락 캐릭터·정렬을 검증하는 테스트 2개, 변경 파일 린트, 프로덕션 빌드가 통과했습니다. 실제 DB 수정 없이 검증했습니다.

`src/app/organizations/[id]/page.tsx:199`은 캐릭터의 `status === 'active'`만으로 현재 멤버를 분류합니다. 상위 organization_members 조회에는 `left_at`도 선택하지 않습니다. 관리자 `setMembersLeft`는 캐릭터 상태가 아니라 `left_at`만 갱신하므로 퇴장 후에도 멤버 카드와 인원 수에 계속 포함됩니다. 조회·타입에 left_at을 포함하고 현재/이전 멤버를 구분해야 합니다.

### 2. 지도·스트리머 저장 실패를 성공처럼 처리 — 높음

- `src/app/admin/map/actions.ts:13,25,41,51,58`: 저장·삭제 결과의 error를 확인하지 않습니다. AdminMapView는 액션 완료 후 입력·선택을 초기화합니다.
- `src/app/admin/streamers/actions.ts:14,21,33,39`: 추가·변경·삭제 오류를 무시합니다. StreamerEditRow는 저장 후 편집을 종료하며 예외 시 로딩 해제도 보장하지 않습니다.
- 재현 조건: 중복 채널 ID, DB 제약 위반 또는 서비스 오류. 오류 반환과 UI 메시지, 실패 시 입력 유지, finally를 통한 로딩 해제가 필요합니다.

### 3. 여러 단계 생성 작업의 부분 성공 — 높음

`src/app/admin/characters/actions.ts:50`은 캐릭터 생성 후 소속 추가 실패를 무시하고 success를 반환합니다. 스트리머 추가도 미정 캐릭터 생성 실패 시 스트리머만 남습니다. 트랜잭션 RPC 등으로 두 작업을 원자적으로 처리하고 실패를 사용자에게 전달해야 합니다.

### 4. 제보 추가 필드 검증 누락 — 높음

`src/app/report/actions.ts:63`은 clip_report를 string[]으로 단언하고 trim을 호출합니다. File을 보내면 `u.trim is not a function`이 발생하며 이 코드는 try/catch 밖입니다. 또한 본문만 2,000자로 제한하고 추가 클립·참여 인물·스트리머 입력과 합친 최종 본문은 검증하지 않습니다. 모의 실행에서 10,000자 clip_report가 success와 10,016자 저장 payload를 생성했습니다. 모든 FormData 값의 실제 타입, 개수, 길이 및 최종 합산 길이를 검사해야 합니다. 좌표도 Number.isFinite 및 지도 범위 검사가 필요합니다.

### 5. 공개 상세·관련 화면의 캐시 무효화 누락 — 중간

조직 멤버 액션의 paths는 조직 화면만 무효화해 캐릭터·스트리머 화면이 즉시 갱신되지 않습니다. 지도 위치 액션은 `/map`, `/admin/map`만 무효화해 조직·캐릭터 미니맵에 이전 위치가 남을 수 있습니다. 스트리머 액션도 공개 경로를 무효화하지 않습니다. 엔티티 변경에 영향을 받는 공개 상세·목록을 함께 갱신해야 합니다.

### 6. 클립 전환 시 이전 영상 재생이 계속될 수 있음 — 중간

`src/components/events/ClipPlayer.tsx:73,97`은 모든 iframe을 유지하고 visibility만 변경합니다. 이미 재생 중인 영상은 전환 시 pause 또는 unmount되지 않습니다. autoplay 미설정은 최초 자동 재생만 막으므로 이전 클립 소리가 겹칠 수 있습니다. 활성 iframe만 유지하거나 공급자 플레이어 API로 비활성 클립을 정지해야 합니다. 전체 iframe 선로딩은 방문자의 네트워크 비용도 늘립니다. 브라우저에서 실제 공급자별 재생 동작은 추가 확인이 필요합니다.

### 7. 테스트 재현성과 린트 실패 — 중간

- `.gitignore:45`가 테스트 스크립트를 제외합니다. `git ls-files scripts`에는 업로드 스크립트만 있어 새 clone에서 package.json의 test 명령을 실행할 수 없습니다.
- 제보 테스트는 새 next/headers·차단 IP 의존성에 맞게 mock을 갱신해야 합니다.
- 린트 오류: ClipsEditor.tsx:130, ParticipantsEditor.tsx:113, ReportForm.tsx:27,103,191,258의 effect 내 동기 setState. 상태 초기화·prop 동기화 방식을 정리해야 합니다.

## 추가 검토 항목

- schema.sql:174의 reports 공개 INSERT 정책은 `with check (true)`입니다. 운영 DB에도 동일하다면 REST 직접 요청은 서버 액션의 IP 차단·허니팟·입력 검사를 거치지 않습니다. 운영 정책과 권한 확인 후 서버 경유 쓰기 또는 DB 검증을 적용해야 합니다. 이번 점검에서 운영 정책 적용 여부는 확인하지 않았습니다.
- schema.sql:170~171은 event_participants/event_clips를 무조건 공개합니다. 비공개 사건의 참여자·클립까지 숨겨야 한다면 부모 사건의 공개 여부를 RLS에 반영해야 합니다. 운영 정책 확인 필요.
- addOrgMembers/updateOrgMember는 다른 조직의 is_primary를 해제하지 않습니다. 주 소속을 하나로 제한할 의도라면 서버와 DB에서 불변 조건을 보장해야 합니다.
- reorderMembers의 행별 병렬 UPDATE는 일부만 성공할 수 있습니다. 사건 순서 저장도 같은 원자성 기준으로 검토할 필요가 있습니다.
- PROJECT.md는 Next.js 15+로 기술하지만 설치 버전은 16.3.4입니다. 또 4단계 중첩 쿼리 문제가 일반적인 PostgREST 한계로 단정되어 있습니다. 프로젝트에서 관찰한 현상과 재현 조건으로 한정해 기록하는 편이 정확합니다.

## 권장 순서

퇴장 멤버 표시 → 관리자 저장 오류·생성 원자성 → 제보 검증과 운영 RLS 확인 → 캐시 무효화 → 테스트 복구·린트 → 클립 재생 관리 순서로 수정합니다.
